export const InstructionUnit = ({children}) => {
    return (
        <div class="unit-rule flex">
                    <span class="material-icons star-icon"> star </span>
                    <p class="font-md">{children}</p>
                </div>
    )
}