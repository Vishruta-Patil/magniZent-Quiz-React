const MainTitle = ({ children }) => {
    return (
    <div>
        <h2 class="font-lg black-color category-header font-header">
            {children}
        </h2>
        <div class="underline-header"></div>
    </div>
)}

export default MainTitle